import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'front_quimica',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
